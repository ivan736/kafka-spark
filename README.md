# A demo project testing spark streaming

* This project creates containers for `zookeeper`, `kafka`, `spark` and `python` to provide a simple test environment.
* Todo: test spark structured streaming